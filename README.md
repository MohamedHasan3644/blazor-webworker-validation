# WebWorkerClient cancellation and timeout validation

Customer-style sample and manual validation plan for
[dotnet/aspnetcore#68819](https://github.com/dotnet/aspnetcore/issues/68819).

The sample uses only public templates and APIs. It provides a visible,
interactive console for worker creation, invocation, cancellation, timeout,
disposal, concurrency, recovery, and worker-leak checks.